<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.ronalders</groupId>
        <artifactId>operaton-process-test-coverage-root</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <name>Operaton Process Test Coverage Regression Tests Parent</name>
    <artifactId>operaton-process-test-coverage-regression-tests-parent</artifactId>
    <packaging>pom</packaging>

    <properties>
        <graalvm.version>25.0.0</graalvm.version>
        <operaton.version>${operaton.compile.version}</operaton.version>

        <jacoco.skip>true</jacoco.skip>
        <dokka.skip>true</dokka.skip>
        <maven.javadoc.skip>true</maven.javadoc.skip>
        <maven.source.skip>true</maven.source.skip>
        <maven.install.skip>true</maven.install.skip>
        <gpg.skip>true</gpg.skip>
    </properties>

    <modules>
        <module>junit5</module>
        <module>spring-test</module>
        <module>spring-starter</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Re-define Kotlin (needed because otherwise will be overwritten by spring boot) -->
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-bom</artifactId>
                <version>${kotlin.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Re-define Spring Boot import -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>

            <dependency>
                <groupId>org.operaton.bpm</groupId>
                <artifactId>operaton-bom</artifactId>
                <version>${operaton.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>

            <!-- Own BOM -->
            <dependency>
                <groupId>io.github.ronalders</groupId>
                <artifactId>operaton-process-test-coverage-bom</artifactId>
                <version>${project.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.graalvm.polyglot</groupId>
            <artifactId>js</artifactId>
            <version>${graalvm.version}</version>
            <scope>runtime</scope>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>org.graalvm.js</groupId>
            <artifactId>js-scriptengine</artifactId>
            <version>${graalvm.version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!-- Profiles just here to ease testing in IDEs -->
        <profile>
            <id>operaton-bpm-engine-1.1.2</id>
            <properties>
                <operaton.version>1.1.2</operaton.version>
                <spring-boot.version>3.5.6</spring-boot.version>
            </properties>
        </profile>
        <profile>
            <id>operaton-bpm-engine-2.1.0</id>
            <properties>
                <operaton.version>2.1.0</operaton.version>
                <spring-boot.version>4.0.6</spring-boot.version>
            </properties>
        </profile>

    </profiles>

</project>
